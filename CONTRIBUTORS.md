# Contributors

The following engineering teams and roles have participated in the development of Omni Suite:

## Engineering Roles

- **Principal Full-Stack Engineer / AI Coding Agent**: Primary implementation authority responsible for code correctness, database schemas, security hardening, and structural performance. Active on session `2026-07-14` implementing flat-map attributes, suggested keys, and backward-compatible data normalization; session `2026-07-15` introducing Sorensen-Dice fuzzy matching, AI confidence scores, diagnostic root cause analysis, end-to-end performance timing telemetry, and high-resilience multi-model fallback failovers; and session `2026-07-16` implementing dual-stage progress reporting, HTML5 canvas image downsampling/compression, PDF warnings, and AbortController timeout guards, followed by comprehensive data integrity enhancements (enforcing strict JSON Schema string enums for confidence scores, multi-key unit price extraction, and direct category-attribute mapping integration), session-saving high-speed consecutive entry capabilities (global toast notification system, Save & Close/Add Another modal options, and custom GCS CORS bucket configurations), and introducing the splitscreen interactive Side-by-Side Document Preview panel, Admin-only in-dropdown product category additions with catalog integrity preservation overlays (case-insensitive deduplication, advisory warnings, audit trails, and automatic Firestore synchronization), same-origin Blob URL conversion with high-performance caches to bypass iframe sandboxing security protections, and robust file-extension analysis fallback routines to guarantee native `<img>` rendering for upload fallbacks. Also logged comprehensive documentation on sandbox console accessibility, Firebase storage configurations, and local file storage Sync limit calculations, optimized the application to instantly bypass Firebase Cloud Storage upload attempts in Starter Tier projects, ensuring an instant, zero-cost, zero-delay local Base64 storage fallback directly integrated with Firestore, integrated a client-side dynamic canvas-based PDF.js document viewer to bypass native browser/iframe sandboxing blocks with interactive page and zoom control rails, and updated the automatic backoff retry pipeline to target the active, highly stable `gemini-3.1-flash-lite` model, fully resolving 404 Model Not Found failover exceptions. Continued into session `2026-07-20` to increase the client-side `AbortController` timeout from 40s to 90s, preventing premature disconnection aborts during high-congestion server-side retry/failover operations, designed a server-side model-rotating backoff handler cycling between `gemini-3.5-flash`, `gemini-3.1-flash-lite`, and `gemini-flash-latest` to route through busy API routes gracefully, enabled global inline account and contact editing ("✎ Edit Details") right within the active form viewport with global document synchronization, built independent sort toggles (Default, A-Z, Z-A) next to labels for four critical select dropdown lists, and coupled the AI Document Autofill routine with the global toast notification engine to render rich performance, matched-company, and items-count success or failure visual feedback; and redesigned the "Account Pairing" section actions to use space-saving, animated vertical actions dropdown menus (`MoreVertical` icon) to eliminate overlapping overflows and aligned all text labels across Log Metadata, Proposal Identifiers, and Line Items using a synchronized flexbox structure to deliver an extremely clean and highly presentable workspace. In session `2026-07-22`, upgraded the primary server-side model pool to `gemini-3.6-flash` with fallbacks to `gemini-3.1-flash-lite` and `gemini-flash-latest`, fully resolving transient 503 high demand traffic spikes on legacy model aliases. Subsequently conducted a pixel-perfect form alignment refactoring in `EnquiryForm.tsx` (version 0.9.3), isolating Line Item card remove buttons into dedicated header bars to eliminate label collisions, implementing flexible attribute key selects with tooltips and truncation to prevent text overflow, standardizing section grid breakpoints for split-screen viewports, and enforcing strict 2-decimal-place currency formatting across all calculated totals. In session `2026-07-23` (version 0.9.4), created the interactive `MarqueeLabel` component with automatic DOM overflow detection and smooth hover-triggered sliding marquee animations (`animate-marquee-hover` keyframes), replacing static labels across all 7 form sections and inline modals to achieve zero layout shifts, prevent text wrapping, and provide complete, elegant label legibility across all viewport sizes. In session `2026-07-23` (version 0.9.5), expanded the Gemini AI extraction engine and response schema in `/server.ts` to automatically extract proposal subjects, customer reference codes, and quotation reference numbers, and updated `EnquiryForm.tsx` to automatically populate these state fields during document uploads. In session `2026-07-23` (version 0.9.6), optimized Gemini extraction heuristics in `/server.ts` and created the interactive **"📋 Paste Excel Row / Raw Text"** modal in `EnquiryForm.tsx` to seamlessly parse tab-delimited multi-column Excel copy-pastes and multi-line commercial specs pricing tables directly into structured form fields and line items. In sessions `2026-07-23` (versions 0.9.7 & 0.9.8), embedded few-shot training examples and exact 20-column Excel header sequence mappings (`S/N #`, `Quote Ref No`, `Listed`, `Received Date`, `Sales Person`, `Customer Name`, `Contact Person`, `Email`, `Landline`, `Mobile`, `Country`, `City / Area`, `Customer Ref`, `Product Type`, `Product Detail`, `Value`, `Projected Order Date`, `Status`, `Remarks`, `Payment Status`) into `/server.ts`, in session `2026-07-23` (version 0.9.9), implemented the Smart Paste Interactive Left Preview Panel with interactive field token chips, dynamic form width calculation (`max-w-6xl` when collapsed), and toggleable preview minimization; and in session `2026-07-23` (version 0.10.0), implemented interactive token scroll-to-view auto-scrolling with pulsing emerald glowing ring feedback, created the dynamic key-value Custom Project Specifications & Info section with quick preset tags, built the unregistered entity AI detection confirmation card with interactive field destination toggles (email & phone destination routing) and one-click database catalog registration, and mapped all extracted tokens to target DOM element IDs across form sections.
In session `2026-08-05` (version 0.22.0), implemented a global stateful dialog overlay confirmations framework to bypass browser iframe/sandboxing restrictions blocking native `window.confirm` and `alert` dialogs, restoring non-working settings edits/deletions and call history deletions. Also refactored the "Geography / Region" field into a fully typing-enabled "Location (Company Registered)" free-text input and updated display icons to MapPin across call queue views.

In session `2026-08-05` (version 0.21.0), expanded the operator call queue item card options to include inline View Details, Edit, and Delete action buttons, decoupled call outcomes to remain fully editable and save-able across all Call Statuses, resolved geography state leaking issues through comprehensive form state reset handlers, added company geography auto-matching, and integrated simulated offline/quota limits checks inside the cascade dropdown option renaming transaction batches.
In session `2026-08-05` (version 0.23.0), implemented immutable system-default dropdown controls (non-editable and non-deletable) for primary Call Statuses and Call Outcomes with an elegant, system-wide visual badge indicator. Reconstructed the Call Center PDF & CSV export engine to dynamically construct report filters, eliminating stats skew and fixing report/CSV export inaccuracies.
In session `2026-08-06` (version 0.24.0), introduced multi-select marking for contacts in the People & Key Contacts Directory across both Card and Table view modes, along with a Bulk Actions Toolbar supporting Batch Delete with confirmation & audit logging, Bulk Company Reassignment modal, Bulk DNC Flagging, and Selected Contacts CSV Export. Refactored individual contact deletion in ContactModal and Company360Modal for complete state synchronization.
In session `2026-08-06` (version 0.25.0), introduced an explicit "Add Myself to Team Roster" action button and roster status indicator in SalespersonProfiles, synchronized onboarding profile setups in UserProfileModal directly with the team member roster collection, and unlocked unrestricted team member deletion by removing single-salesperson length guards and adding direct detail panel header deletion buttons with audit logging.
In session `2026-08-06` (version 0.26.0), unlocked unassigned and independent contact saving by removing blocking `companyId` validation guards, ensured global contact form instantiation defaults to unassigned (`""`) rather than forcibly assigning the first company, and updated the People Directory to render independent personnel with clear `(Unassigned / Independent)` badges.
In session `2026-08-06` (version 0.27.0), over-hauled deletion safety and error logging by upgrading `safeDeleteDoc` in `src/firebase.ts` with strict `cleanId` string validation, diagnostic logging, and boolean success indicators; re-architected `handleDeleteEnquiry` and `handleBulkDeleteEnquiries` in `App.tsx` with instant local state & cache purging (`omni_enquiries`) before executing Firestore operations; and upgraded contact, company, product, and salesperson delete handlers across all components with fallback ID checks (`e.id || _id`), instant state removal, and structured audit logs.
In session `2026-08-06` (version 0.27.1), isolated Gemini API proxy 401 unauthenticated errors by implementing lazy client initialization (`getGeminiClient`), short-circuit key checks, and non-retryable 401 trapping in `server.ts`, while upgrading client-side extraction error handling in `EnquiryForm.tsx` to handle missing/unconfigured API key notices gracefully without dumping console error stack traces. Also fixed missing `recordAuditLog` module import in `src/App.tsx`.
In session `2026-08-06` (version 0.27.2), added an interactive Gemini API Key configuration guidance banner inside `EnquiryForm.tsx` providing step-by-step instructions on setting `GEMINI_API_KEY` in AI Studio Settings, along with a direct action button to switch to Smart Paste (which operates 100% offline with zero API key requirement).
In session `2026-08-06` (version 0.28.0), built the Personal Gemini API Key (BYOK) architecture allowing operators to save and manage personal Google AI Studio keys in local storage (`omni_user_gemini_api_key`), complete with a dedicated `GeminiKeyModal` manager, a one-click direct action button to `https://aistudio.google.com/app/apikey`, custom `x-user-gemini-api-key` header proxy support in `server.ts`, and interactive configuration cards in System Settings (`SettingsHub.tsx`) and extraction failure banners (`EnquiryForm.tsx`).
In session `2026-08-06` (version 0.28.1), optimized Gemini rate limit and credit depletion error handling by updating `server.ts` to immediately bypass backoff retries when encountering 429/RESOURCE_EXHAUSTED errors, and upgrading `EnquiryForm.tsx` to handle quota depletion smoothly with direct action buttons for personal API key setup or offline Smart Paste parsing.
In session `2026-08-06` (version 0.29.0), implemented workspace deletion with trash action button and custom confirmation card UI in `WorkspaceManagerModal.tsx`, created an "API & Database Health" subtab in `SettingsHub.tsx` with live API ping testing, response latency measurement, and Firestore document/quota metrics cards, added admin governance toggles (`ALL_DATA` vs. `OWN_DATA_ONLY` visibility and salesperson assignment locks), and replaced all remaining native `window.confirm` modal calls with custom React state-based confirmation overlays across the codebase to resolve iframe sandbox `allow-modals` restriction warnings.
In session `2026-08-07` (version 0.32.0), implemented "Concerned Person" multi-select team member tagging in `EnquiryForm.tsx` and updated `types.ts` and `permissions.ts` to grant click/inspection access to tagged concerned persons. Refactored the Companies & Contacts Outreach History into a retractable side panel in `CompanyModal.tsx` matching `CallLogManager.tsx`, standardized owner labels to "Logged by: *Name*", enabled click-to-inspect modals for call and proposal history records, and audited/removed doubled text emojis.
In session `2026-08-08` (version 0.35.0), executed a comprehensive full-stack and codebase audit, verifying zero-error TypeScript static type validation (`npx tsc --noEmit`) and clean production build bundling (`npm run build`). Synchronized version badges across `CloudSyncHub.tsx` (`v0.35.0`) and updated `DocsSystemHub.tsx` with full release timeline entries up to version `0.35.0`.
In session `2026-08-08` (version 0.36.0), executed a major architectural refactor to resolve Firestore credit/quota depletion. Transformed the data sync pipeline into a Local-First Repository Architecture with client-side IndexedDB caching (`src/services/db.ts`) and a write-ahead `SyncEngine` background worker (`src/services/SyncEngine.ts`). Built typed repository layers (`EnquiryRepository`, `CompanyRepository`, `CallLogRepository`, `MetadataRepository`), eliminated recursive snapshot write loops in `App.tsx` (`syncSNNumbersInFirestore`, dropdown auto-seeding, `audit_logs` socket), updated `firebase.ts` write primitives to fail-safe enqueue to IndexedDB, transformed `CloudSyncHub.tsx` into a System Health & Connectivity Monitor, and added on-demand S/N batch re-indexing in `SettingsHub.tsx`. In session `2026-08-08` (version 0.37.0), implemented Soft-Delete architecture across core entities (`Enquiry`, `Company`, `Contact`, `Product`, `CallLogEntry`), created `TrashBinModal.tsx` Recycle Bin & Data Recovery Hub, added soft delete and recovery methods across all repository layers, integrated quick-access Recycle Bin action in `Sidebar.tsx`, and updated UI list views to exclude deleted records while preventing offline delete/edit conflict data loss. In session `2026-08-08` (version 0.38.0), implemented Phase 1 Call Logger refinements across `CallLogManager.tsx` and `CallLogReportModal.tsx`: added `handleChannelSwitch` to reset status/outcome on tab switches, expanded 1-Click Quick-Create form with full company metadata fields (City/Area, Country, Heat Temperature, Phone Label, Designation), enhanced company/contact selection with auto-fill and `-- Direct Company Line --` options, and aligned CSV and PDF export generators to explicitly include `Interaction Channel / Mode`. In session `2026-08-08` (version 0.40.0), performed a full-codebase UI, UX, and Design System Audit across all React components in `/src/components/`. Standardised modal overlay geometry (`max-h-[85vh]`, `max-w-2xl`/`max-w-4xl`, `flex flex-col`, `flex-1 overflow-y-auto`) across `ContactDetailModal.tsx`, `CallLogDetailModal.tsx`, and `Company360Modal.tsx`; clamped history side panel width in `CompanyModal.tsx` (`w-80`/`w-96` on `xl`/`2xl`) to preserve dominant flex space for the Companies Registry table; wired `auditLogs` into `CloudSyncHub.tsx` state backup/restoration routines and IndexedDB storage; built the Security Audit Trail & Activity Log UI in `SettingsHub.tsx` featuring instant search, action filtering (`CREATE`, `UPDATE`, `DELETE`), CSV export generation, and printable PDF report generation; and bumped application version to `v0.40.0`.
 In session `2026-08-08` (version 0.40.1), fixed the Invite Code Consumption and Workspace Linking flow in `WorkspaceManagerModal.tsx`. Built an atomic multi-record write chain (`writeBatch` with fallback to `safeSetDoc`/`safeUpdateDoc`) updating `invites` (`is_used: true`, `claimed_by_uid`, `claimed_by_email`, `claimed_at`), `users` (`workspaceIds`, `defaultWorkspaceId`, `role`), and `workspaces` (`members` array with `uid`, `email`, `name`, `role`, `joined_at`). Triggered immediate UI state updates (`onWorkspacesChange`, `onProfileUpdated`, `onSelectWorkspace`) to auto-switch the user's active workspace and display the user in the Admin's Workspace Team Members list.
 In session `2026-08-08` (version 0.40.2), eliminated the `Uncaught TypeError: Cannot read properties of undefined (reading 'substring')` white screen crash in published builds by adding defensive string fallbacks `(ws?.name || 'WS').substring(0, 2).toUpperCase()` across `WorkspaceManagerModal.tsx` workspace mapping loops. Added safe optional chaining and string fallbacks to user/workspace property accesses across `Sidebar.tsx`, `App.tsx`, `UserManagementHub.tsx`, and `UserProfileModal.tsx`, sanitized `UserProfile` and `WorkspaceMember` payload creation in `WorkspaceManagerModal.tsx` and `Login.tsx`, and hardened `getInitials` in `src/types.ts`.
In session `2026-08-08` (version 0.41.0), refactored the multi-tenant permissions architecture to implement workspace-scoped roles (`workspace_roles?: Record<string, UserRole>`). Updated `getUserRoleInWorkspace` and `isWorkspaceAdmin` in `src/utils/permissions.ts` to evaluate permissions against `activeWorkspace.id`, ensuring a user can hold Admin privileges in Workspace A while joining Workspace B as a Member/Viewer. Tagged invite codes with `workspace_id` in `InviteManager.tsx` and updated `handleRedeemJoinCode` in `WorkspaceManagerModal.tsx` to prevent global role escalation. Implemented real account deletion in `SettingsHub.tsx`, purging user records from all workspace member rosters, deleting user Firestore documents, wiping IndexedDB stores and LocalStorage caches via `clearAllLocalStores()`, and executing Firebase Auth sign-out.
In session `2026-08-08` (version 0.42.0), resolved cross-user invite redemption sync and Firestore permission bypass issues. Refactored `WorkspaceManagerModal.tsx` `handleRedeemJoinCode` to execute user document updates FIRST, replaced batch writes with error-trapped sequential safe writes (`safeSetDoc` -> `safeUpdateDoc` -> `safeSetDoc`), and added a direct fresh workspace metadata re-fetch (`safeGetDoc('workspaces', targetWsId)`). Added read-only `onSnapshot` listeners in `InviteManager.tsx` (`invites` collection) and `UserManagementHub.tsx` (`workspaces/{activeWorkspace.id}` document) so invite token status ("Claimed by [Email/User]") and team member roster updates propagate in real-time across active admin screens without requiring page refreshes.
In session `2026-08-08` (version 0.43.0), hardened Firestore document merge primitives in `src/firebase.ts` by defaulting `safeSetDoc` options to `{ merge: true }` and adding fallback merge writes to `safeUpdateDoc`. This prevents document field truncation during invite code redemption or user profile updates. Expanded `userUpdatePayload` and `workspaceUpdatePayload` in `WorkspaceManagerModal.tsx` to retain full user profile details (`...currentUser`) and complete workspace metadata (`name`, `created_by`, `createdAt`, `modules`, `members`), auto-healing missing fields in Firestore.
In session `2026-08-08` (version 0.44.0), refactored `getUserRoleInWorkspace` in `src/utils/permissions.ts` to preserve global/top-level Admin status across all workspaces unless explicitly mapped as a non-admin role in `user.workspace_roles[targetWsId]`. Updated `handleRedeemJoinCode` in `WorkspaceManagerModal.tsx` to preserve the user's primary `defaultWorkspaceId` when joining secondary workspaces. Aligned `useMemo` workspace data filtering hooks in `src/App.tsx` (`companies`, `contacts`, `enquiries`, `salespersons`, `products`, `callLogs`) to allow unassigned/legacy records without `workspace_id` to render when viewing the default/primary workspace. In session `2026-08-09` (version 0.45.0), implemented workspace memory persistence (`localStorage`) and active workspace badge UI in `src/App.tsx`, added pre-deletion data breakdown analysis and dual backup/purge confirmation workflows ("Download JSON Backup & Purge Workspace", "Purge Without Backup") with client-side JSON backup generation in `WorkspaceManagerModal.tsx`, and built the workspace-scoped Follow-Up Radar Command Center widget in `Dashboard.tsx` with Overdue, Due Today, and Upcoming queues and quick action triggers. In session `2026-08-09` (version 0.46.0), built the "✨ AI Assist" Toolbar in `QuickActivityDrawer.tsx` featuring 1-click "Summarize Notes" and "Draft WhatsApp Message" with `wa.me` dispatch, backed by a new `/api/gemini/quick-assist` Express endpoint in `server.ts` with personal Gemini API key pass-through and model fallback retry logic. In session `2026-08-09` (version 0.47.0), added Activity Velocity & Performance Analytics to `Dashboard.tsx` featuring touchpoint channel breakdowns across Call, WhatsApp, Email, Meeting, and Site Visit with date range filters, Follow-up Compliance Score gauge calculation, Top Sales Rep Leaderboard, and Pipeline Conversion Funnel widget with stage counts, conversion percentages, and AED pipeline values. In session `2026-08-09` (version 0.48.0), created standard `PageHeader.tsx` layout component, applied it across all top-level page views (`Dashboard.tsx`, `CallLogManager.tsx`, `EnquiryList.tsx`, `CompanyModal.tsx`, `SalespersonProfiles.tsx`, `ProductManager.tsx`, and `SettingsHub.tsx`), and fixed duplicate React key warnings when rendering sales representatives. In session `2026-08-12` (version 0.62.0), implemented auto-sync disposition logic in `QuickActivityDrawer.tsx` binding Call Status buttons to `outcome` text state, added strict duplicate company name checks in `ActivityLogRepository.convertUnsavedLeadToClient` to guard against duplicate creation during lead conversions, ensured explicit registration and transaction error handling for all IndexedDB object stores in `db.ts`, and unified all sub-modals across `CompanyModal.tsx` and `ContactModal.tsx` to the Slate Dark UI standard. In session `2026-08-12` (version 0.63.0), completed the System-Wide UI Uniformity & Queue Management Overhaul: refactored `QuickActivityDrawer.tsx` with automated form hydration for editing existing logs (`existingLog` / `logToEdit`) and conditional update routing, added a "Scheduled" status preset and automated `completedAt` timestamp recording when transitioning scheduled calls to completed, eradicated native `<select>` dropdowns for statuses and outcomes across `QuickActivityDrawer.tsx` and `CallLogManager.tsx` in favor of a uniform 1-Tap Pill Grid, modernized the Fast Outcome Logger drawer in Slate Dark theme with Missing Lead Guard inline fields and Smart Date Toggle chips (`Tomorrow`, `Next Week`, `None`), and updated edit triggers across `CallLogDetailModal.tsx`, `CompanyModal.tsx`, `CallLogManager.tsx`, and `Dashboard.tsx` to pass `existingLog`. In session `2026-08-14` (version 0.65.0), completed Phase 3 DNC & Temperature Restructure: extended `CompanyTemperature` model to include `'DNC'`, upgraded 3-stage heat badges to 4-stage interactive cycling (Cold ❄️ -> Warm 🌤️ -> Hot 🔥 -> DNC 🚫) across `CompanyModal.tsx` and `Company360Modal.tsx` with automated `is_dnc` setting, cleaned up company form inputs by removing standalone DNC checkboxes, and modernized the contact DNC restriction UI in `ContactModal.tsx` with a toggle switch, alert pill, and reason notes input. In session `2026-08-14` (version 0.66.0), completed Phase 4 UI Polish, Quick Actions, & Purpose Dropdowns: injected dynamic leading interaction symbols (`PhoneCall`, `Mail`, `MessageSquare`, `Calendar`) into log entries across Operator Queue, History table, Activity Details, and Company 360 Call Operations; added 1-click Quick Action mini-buttons (Dial, Email, WhatsApp) to contact cards in `Company360Modal.tsx` with non-digit stripping regex for WhatsApp URLs; integrated date sorting toggles ("Date: Oldest First" / "Date: Newest First") in `CallLogManager.tsx` for Operator Queue and Call History views; and expanded `SYSTEM_CALL_PURPOSES` presets in `defaults.ts` while preserving custom values seamlessly across `CallLogDetailModal.tsx` and `QuickActivityDrawer.tsx`.
- **Operator (User)**: Provided functional requirements and environment verification feedback.
